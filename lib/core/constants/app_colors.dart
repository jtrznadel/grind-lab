import 'package:flutter/material.dart';

class AppColors {
  //primitive colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  //surface colors
  static const Color backgroundColor = Color(0xFFF4F3F5);

  //primary colors
  static const Color primary = Color(0xFF181619);

  //text colors
  static const Color textPrimary = Color(0xFF181619);
  static const Color textSecondary = Color(0xFF8B8A8E);

  //icon colors
  static const Color iconPrimary = Color(0xFF181619);
  static const Color iconSecondary = Color(0xFF8B8A8E);
}
